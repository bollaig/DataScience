install.packages(pwr)
install.packages(power)
install.packages(pwr)
install.packages("pwr")
library(pwr)
power_changes <- pwr.p.test(h = ES.h(p1 = .75, p2 = .5), sig.level = .05, power = .8, alternative = "greater")
plot(power_changes)
power_changes
power_changes <- pwr.p.test(h = ES.h(p1 = .75, p2 = .5),                             sig.level = .01, power = .8,                             alternative = "greater")
plot(power_changes)
power_changes
power_changes <- pwr.p.test(h = ES.h(p1 = .75, p2 = .5),                             sig.level = .01,                              alternative = "greater")
plot(power_changes)
power_changes
power_changes <- pwr.p.test(h = ES.h(p1 = .75, p2 = .5),                             sig.level = .01, n=40,                             alternative = "greater")
plot(power_changes)
power_changes
power_changes <- pwr.p.test(h = ES.h(p1 = .75, p2 = .5),                             sig.level = .01, n = 40)
plot(power_changes)
power_changes
power_changes <- pwr.p.test(h = ES.h(p1 = .65, p2 = .5),                             sig.level = .05, power = .8,                             alternative = "greater")
plot(power_changes)
power_changes
power_changes <- pwr.p.test(h = ES.h(p1 = .75, p2 = .5),                             sig.level = .05, power = .8,                             alternative = "greater")
power_changes
effect_size <- cohen.ES(test="r", size="medium")
effect_size
effect_size <- cohen.ES(test = c("r", "anov"), size = "medium")
effect_size <- cohen.ES(test = c("r"), size = "medium")
effect_size
effect_size <- cohen.ES(test = c("r"), size = c("medium"))
effect_size
pwr.r.test(r = effect_size$effect.size, power = .8, sig.level =.5)
effect_size <- cohen.ES(test = "r", size = "medium")
pwr.r.test(r = effect_size$effect.size, power = .8, sig.level =.5)
effect_size <- cohen.ES(test = c("r"), size = c("large"))
pwr.r.test(r = effect_size$effect.size, power = .8, sig.level =.5)
effect_size <- cohen.ES(test = c("r"), size = c("small"))
pwr.r.test(r = effect_size$effect.size, power = .8, sig.level =.5)
effect_size <- cohen.ES(test = c("r"), size = c("medium"))
effect_size <- cohen.ES(test = c("r"), size = c("medium"))
pwr.r.test(r = effect_size$effect.size, power = .8, sig.level =.5)
pwr.r.test(r = effect_size$effect.size, power = .8, sig.level =.05)
effect_size <- cohen.ES(test = c("r"), size = c("small","medium"))
effect.sizes <- c("0.2", "0.5", "0.8")
power_changes <- pwr.p.test(h = effect.sizes, n=20,                             sig.level = .05,                             alternative = "greater")
plot(power_changes)
effect.sizes <- c(0.2, 0.5, 0.8)
power_changes <- pwr.p.test(h = effect.sizes, n=20,                             sig.level = .05,                             alternative = "greater")
plot(power_changes)
power_changes
effect.sizes <- c(0.2, 0.5, 0.8)
power_changes <- pwr.p.test(h = effect.sizes, n=20,                             sig.level = .05,                             alternative = "greater")
plot(power_changes)
power_changes
?pwr.2p.test
power_changes <- pwr.2p.test(h = ES.h(p1 = .65, p2 = .5),                             sig.level = .05, power = .8,                             alternative = "greater")
power_changes
power_changes <- pwr.2p.test(h = ES.h(p1 = .55, p2 = .5),                             sig.level = .05, power = .8,                             alternative = "greater")
power_changes
power_changes <- pwr.2p.test(h = ES.h(p1 = .55, p2 = .5),                             sig.level = .05, power = .8)
power_changes
                            alternative = "less")
power_changes
power_changes
power_changes <- pwr.2p.test(h = ES.h(p1 = .55, p2 = .5),                             sig.level = .05, power = .8,                             alternative = "less")
power_changes <- pwr.2p.test(h = ES.h(p1 = .50, p2 = .55),                             sig.level = .05, power = .8,                             alternative = "less")
power_changes
